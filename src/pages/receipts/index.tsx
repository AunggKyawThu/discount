import { type NextPage } from "next";
import { Layout } from "../../components";

const Receipts: NextPage = () => {
  return (
    <Layout title="Receipts">
      <p>This is receipts page.</p>
    </Layout>
  );
};

export default Receipts;