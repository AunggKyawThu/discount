import { type NextPage } from "next";
import { Layout } from "../../components";

const Transfers: NextPage = () => {
  return (
    <Layout title="Transfers">
      <p>This is transfers page.</p>
    </Layout>
  );
};

export default Transfers;